import math

a=float(input('a?'))
b=float(input('b?'))
c=float(input('c?'))

if a<b+c and b<a+c and c<a+c:
   s=(a+b+c)/2
   m={s*(s-a)*(s-b)*(s-c)}**2
   print(f'三角形の面積:{m}')
   else:
   print(f'三角形になりません')
